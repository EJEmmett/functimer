import pytest

from tests.fixtures.timed import (
    mock_timed,
    timed_base,
    timed_disabled,
    timed_estimate,
    timed_exception,
    timed_number,
    timed_return,
    timed_stdout,
    timed_unit,
)
from tests.fixtures.util import timed_result_micro, timed_result_milli
